extends ./layout

block content

    .container.mobile

        textarea#input-modules(rows='3' placeholder="fetch each random...")
        a.btn#build-btn(href='#') BUILD
        a.btn#download-btn.disabled(download='util.js') DOWNLOAD

        ul#build-logger
            li Note:
            li Module names are separated by spaces.
            li The generated library is exported as "_" in global space, using UMD pattern.

        ul#build-modules
            each val, index in licia
                li(title=val.description)= index    

    script(src='#{site.baseUrl}static/js/builder.js')